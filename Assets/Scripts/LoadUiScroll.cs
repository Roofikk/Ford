using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.UIElements;

public class LoadUiScroll : MonoBehaviour
{
    public ScrollRect scrollView;
    public GameObject bonesLoad;

    // Start is called before the first frame update
    void Start()
    {
        //object[] boneGroups = bonesLoad.transform.GetComponentsInChildren<LoadBones>();
        //object[] scrollBoneGroups = scrollView.content.GetComponentsInChildren<VerticalLayoutGroup>();
    }

    public void LoadScroll()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
